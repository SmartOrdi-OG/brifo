# سجل الشغل — Brifo

توثيق لكل شي تم عمله بجلسات الشغل الأخيرة، وشو لسا ناقص.

## ✅ تم إنجازه

### 1. إصلاح إشعارات المواعيد (Push Notifications)
- المشكلة: زر "تفعيل الإشعارات" كان يرجع لوضع "بس والتطبيق مفتوح" (fallback) بدل ما يفعّل push حقيقي.
- السبب الجذري: متغير `VAPID_PUBLIC_KEY` بـ Vercel كان محتوي نص غلط (مو المفتاح الفعلي) بدل قيمة صحيحة.
- ⚠️ اكتشفنا بالطريق تسريب أمني: `/api/push-public-key` كان عم يرجّع `VAPID_PRIVATE_KEY` و `CRON_SECRET` الحقيقيين بالنص الصريح لأي حدا يفتح الرابط (لأنه كانوا جوا نفس النص الغلط المخزّن بـ `VAPID_PUBLIC_KEY`).
- الحل: ولّدنا مفاتيح VAPID جديدة + CRON_SECRET جديد، صلّحنا القيم بـ Vercel (كل متغير لحاله، بدون نص زيادة)، وأكدنا إنه `/api/push-public-key` هلق بيرجع بس المفتاح الصحيح.
- تأكدنا إنه push شغال بعد إعادة التثبيت الصحيح (Add to Home Screen).

### 2. حادثة فقدان بيانات (مواعيد + أطفال)
- صار فقدان بيانات بعد ما انحذفت أيقونة الهوم سكرين وانضافت من جديد — آيفون بيمسح حاوية تخزين الأيقونة القديمة بالكامل (localStorage) لما تنحذف.
- تم استرجاع البيانات وقتها من نسخة احتياطية يدوية (ملف JSON) كانت محفوظة من قبل.

### 3. نظام استرجاع تلقائي سحابي (Cloud Backup) — PR #6
- بيانات التطبيق (الأطفال، الرسائل، المدفوعات، المواعيد، المهام، التقييم) هلق تتخزن تلقائياً على السيرفر (KV)، مربوطة بـ **كود استرجاع** من 12 حرف يعيش خارج الجهاز.
- الإعدادات هلق فيها قسم "استرجاع تلقائي" بيوري الكود (مع زر نسخ)، وخانة لإدخال كود قديم لاسترجاع البيانات منه.
- هيك حتى لو انحذف التطبيق أو تغيّر الجهاز، البيانات ما تضيع — بس المستخدم/ة لازم تكون حافظة الكود بمكان خارج الجهاز (نوت، واتساب لحالها، إيميل).

### 4. جمع تقييمات المستخدمين + صفحة أدمن — PR #7
- قبل هيك، التقييمات (نجوم + تعليق) كانت تتخزن بس محلياً على جهاز كل مستخدم/ة — ما كانت توصل لأي حدا.
- هلق كل تقييم بينبعت كمان لسيرفر (`/api/rating-submit`) وينخزن بقاعدة البيانات (بدون أي معلومة تعرّف عن الهوية).
- صفحة جديدة `/admin` (مش مربوطة بأي زر بالتطبيق، لازم تفتحيها مباشرة بالرابط) بتوريكي كل التقييمات + المعدل العام، محمية بكلمة سر (`ADMIN_SECRET`).
- **لازم تتأكدي إنك ضفتي `ADMIN_SECRET` بمتغيرات Vercel** (تم توليد كلمة سر وإرسالها بالمحادثة) وعملتي Redeploy، وإلا الصفحة مفتوحة بدون كلمة سر لأي حدا يلاقيها.

### 5. حذف المواعيد المنتهية — PR #8
- شاشة التقويم هلق فيها قسم "مواعيد منتهية" يوري أي موعد فات تاريخه (كان قبل هيك يختفي تماماً من كل الشاشات بدون طريقة تحذفيه، بس يضل مخزّن للأبد).
- كل موعد (قادم أو منتهي) هلق فيه أيقونة سلة مهملات 🗑️ تحذفه فوراً.
- تم التأكد إنها شغالة (الحذف بيمسح الموعد فعلياً من التخزين، مش بس بيخفيه بالواجهة).
- ⏳ عندك مشكلة إنك ما لقيتي زر الحذف — على الأغلب بسبب نسخة قديمة محفوظة بالـ Service Worker. الحل: إغلاق التطبيق بالكامل من قائمة التطبيقات المفتوحة وإعادة فتحه.

### 6. تعديل المواعيد — PR #11
- زر تعديل ✏️ جنب زر الحذف بكل موعد بالتقويم — فيك تغيّري العنوان/التاريخ/الطفل المرتبط بدون ما تحذفي وتضيفي من جديد.

### 7. حفظ صورة الرسالة الأصلية — PR #12
- صورة الرسالة يلي بتصوّريها هلق تنحفظ فعلياً (كانت قبل تتحلل وتنمسح فوراً)، فيك ترجعي تفتحيها من ملف الطفل بعدين.

### 8. تاريخ استحقاق اختياري للمهام — PR #13
- فيك تحطي تاريخ استحقاق لأي مهمة بقائمة "المهام"، وبينضم لنفس نظام تذكيرات المواعيد.

### 9. بحث عبر الرسائل والمدفوعات — PR #14
- خاصية بحث جديدة (`/search`) تدوّر بعنوان الرسائل والمدفوعات.

### 10. مزامنة حقيقية بين جهازين بنفس كود الاسترجاع — PR #15
- قبل هيك: كل جهاز كان يكتب فوق نسخة السحابة بالكامل، فلو الأب والأم مستخدمين نفس كود الاسترجاع على جهازين، جهاز كان ممكن يمسح تعديلات الجهاز التاني بالغلط.
- هلق فيه دمج (merge) حقيقي: كل جهاز بيسحب نسخة التاني ويدمجها (عند الفتح، وكل 45 ثانية، ولما يرجع التطبيق يصير مرئي)، بدل ما يكتب فوقها.

### 11. حسابات حقيقية عبر Supabase (رابط دخول بالإيميل) — PR جديد
- التطبيق كامل هلق محجوب خلف تسجيل دخول حقيقي: أول ما حدا يفتح Brifo، لازم يكتب إيميله ويدوس "إرسال رابط الدخول"، وبيستلم رابط دخول بدون كلمة سر (Magic Link) عبر Supabase Auth.
- الإعدادات هلق فيها بطاقة فوق فيها الإيميل المسجّل وزر "تسجيل خروج".
- **بيانات التطبيق (الأطفال، المواعيد...) لسا شغالة بنفس نظام كود الاسترجاع القديم (KV) — ما تغيّر شي فيه بهاي المرحلة.** يعني الحساب الجديد هو طبقة تسجيل دخول فوق النظام الحالي، مش بديل عنه لسا.
- **⚠️ خطوات لازم تعمليها بـ Vercel قبل ما ينشر:**
  1. Settings → Environment Variables → ضيفي `VITE_SUPABASE_URL` = `https://wksrmpkjwnvxbxnwdxsx.supabase.co`
  2. ضيفي `VITE_SUPABASE_ANON_KEY` = المفتاح يلي بعتتيه (anon key — آمن يكون بالكود، مش سري).
  3. بـ Supabase Dashboard → Authentication → URL Configuration → Redirect URLs: ضيفي `https://brifo-drab.vercel.app` (وإلا رابط الدخول ما رح يشتغل صح).
  4. Redeploy بعد ما تضيفي المتغيرات.
- **⏳ لسا ناقص (المرحلة الجاية):**
  - ربط بيانات كود الاسترجاع القديم تلقائياً بالحساب الجديد (Migration) — حالياً لو حدا سجل حساب جديد وعنده كود قديم، لازم يدخل الكود يدوياً من نفس قسم "استرجاع تلقائي" الموجود بالإعدادات.
  - نقل مفتاح تخزين البيانات من "كود الاسترجاع" لـ "هوية الحساب" (user id) بشكل كامل.

### 12. اشتراك Stripe حقيقي (وضع تجريبي Sandbox) + تجربة مجانية 7 أيام — PR جديد
- بوابة دفع Stripe حقيقية (وضع Test — ما في خصم فلوس حقيقي لحد ما نبدّل للمفاتيح الحية): دوسة "اشترك الآن" بصفحة Premium هلق بتفتح صفحة دفع Stripe فعلية (Checkout).
- **تجربة مجانية 7 أيام من تاريخ إنشاء الحساب** — بعدها التطبيق كامل يصير محجوب وبيوري صفحة Premium إجبارياً لحد ما يشترك المستخدم (نفس منطق حجب تسجيل الدخول).
- بعد الاشتراك، Stripe بيبعت إشعار (webhook) للسيرفر يحدّث حالة الاشتراك تلقائياً — ما في تدخل يدوي.
- **⚠️ خطوات لازم تعمليها بـ Vercel + Stripe قبل ما ينشر:**
  1. Vercel → Environment Variables → ضيفي `STRIPE_SECRET_KEY` (القيمة يلي بعتتيها بالمحادثة).
  2. **بعد ما ينشر** (الخطوة دي محتاجة الرابط شغال أول): روحي Stripe Dashboard (Test mode) → Developers → Webhooks → Add endpoint → الرابط: `https://brifo-drab.vercel.app/api/stripe-webhook` → اختاري الأحداث: `customer.subscription.created`, `customer.subscription.updated`, `customer.subscription.deleted`.
  3. بعد ما تضيفي الـ endpoint، Stripe بيوريكي "Signing secret" (يبلش بـ `whsec_...`) — انسخيه وضيفيه بـ Vercel كـ `STRIPE_WEBHOOK_SECRET`.
  4. Redeploy بعد كل خطوة إضافة متغيرات.
  5. **مهم:** هاد كله بوضع Test — لما تكوني جاهزة تاخدي فلوس حقيقية، لازم تبدّلي `STRIPE_SECRET_KEY` و `STRIPE_WEBHOOK_SECRET` بمفاتيح الـ **Live mode** من نفس لوحة Stripe (مش نفس القيم).
- **ملاحظة:** السعر (1.90€/شهر) بينخلق تلقائياً بحساب Stripe تبعك أول مرة حدا يحاول يشترك (Product + Price)، ما في شي لازم تعمليه يدوياً بهاي النقطة.
- لسا لازم تأكيد: نص حق الإلغاء يلي انضاف بـ PR #10 كافي قانونياً، ولا لازم مراجعة إضافية.

### 13. حسابات مجانية دائمة (لصاحبة المشروع) — PR جديد
- إيميلين معفيين بشكل دائم من التجربة/الاشتراك: `ahmadalikaram89@gmail.com` و `kabalana371@gmail.com`.
- **⚠️ لازم تضيفي بـ Vercel:** `FREE_ACCOUNT_EMAILS` = `ahmadalikaram89@gmail.com,kabalana371@gmail.com` (بدون مسافات بعد الفاصلة)، وRedeploy.
- لإضافة إيميل مجاني تالت بالمستقبل، بس عدّلي نفس المتغير وأضيفي فاصلة + الإيميل الجديد — ما بيحتاج تعديل كود.

## 📝 ملاحظات تقنية عامة
- كل الشغل تم على فرع `claude/brifo-reminders-ratings-todos-7g96n1`، وبينضم فوراً بـ Pull Request مدموج (squash) على `main` — نفس الفرع يلي Vercel عم يراقبه وينشر منه.
- أي متغير سرّي جديد (VAPID keys, CRON_SECRET, ADMIN_SECRET) لازم يتضاف يدوياً بـ Vercel → Settings → Environment Variables، وبعدها لازم Redeploy حتى ينعكس.
